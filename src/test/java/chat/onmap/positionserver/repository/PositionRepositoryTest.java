package chat.onmap.positionserver.repository;

import static org.assertj.core.api.Assertions.assertThat;

class PositionRepositoryTest {

//    PositionRepository positionRepository = new LinkedMapPositionRepository();
//
//    @Test
//    void saveAndUpdateTest() {
//        var p1 = new Position(UUID.randomUUID(), 11, 11, LocalDateTime.now());
//        var p2 = new Position(UUID.randomUUID(), 22, 22, LocalDateTime.now());
//        var p3 = new Position(UUID.randomUUID(), 33, 33, LocalDateTime.now());
//        positionRepository.saveOrUpdate(p1);
//        positionRepository.saveOrUpdate(p2);
//        positionRepository.saveOrUpdate(p3);
//
//        var res = positionRepository.findLast(2);
//        assertThat(res.size()).isEqualTo(2);
//        assertThat(res.get(0).getId()).isEqualTo(p3.getId());
//        assertThat(res.get(1).getId()).isEqualTo(p2.getId());
//
//        positionRepository.saveOrUpdate(p1);
//        res = positionRepository.findLast(2);
//        assertThat(res.size()).isEqualTo(2);
//        assertThat(res.get(0).getId()).isEqualTo(p1.getId());
//        assertThat(res.get(1).getId()).isEqualTo(p3.getId());
//
//        res = positionRepository.findAll();
//        assertThat(res.size()).isEqualTo(3);
//    }

}